// palindrom

// function checkPalindrome(inputString) {

// 	var entryControl = ("^[a-z]+$$");

// 	if (inputString.match(entryControl) && inputString.length >= 1 && inputString.length <= 10^5 ) {

// 		var controlArr = [];
// 		var backWord = [];

// 		for (i = 0; i < inputString.length; i++) {
// 			controlArr.push(inputString[i]);
// 		}

// 		for (i = controlArr.length -1; i>=0; i--) {
// 			backWord.push(controlArr[i]);
// 		}

// 		word1 = String(controlArr);
// 		word2 = String(backWord);

// 		if (word1 === word2) {
// 			return true;
// 		}

// 		else {
// 			return false;
// 		}
// 	}
// }


